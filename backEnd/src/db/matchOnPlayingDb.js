const db = require("../../dbConfig/dbConfig")

const sql_batman = ` 

`;

const sql_balling = ` 

`;

function getOnPlaying(io, result){
    db.query(sql_batman,(err, data) => {
        db.query(sql_balling,(err, data) => {

        

        
        });
    });
}